[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orditect-adapter-ui"
version = "0.1.7"
description = "UI adapter reference implementation for the Orditect ecosystem: consumer-side read (trace-bundle) + action sink (HITL/MCP/agent interaction)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [
    { name = "Orditect Contributors" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "orditect-protocol>=0.1.7,<0.2",
    "orditect-flow>=0.1.7,<0.2",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.26",
    "orditect-adapter-local>=0.1.7,<0.2",
    "orditect-adapter-memory>=0.1.7,<0.2",
]

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.setuptools.package-data]
"orditect.adapter.ui" = ["py.typed"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "unit: pure logic tests, no external dependencies",
]