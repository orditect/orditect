[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orditect-adapter-local"
version = "0.0.1"
description = "Placeholder package for orditect-adapter-local"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}
authors = [{name = "Orditect"}]
