[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orditect-bridge-langchain"
version = "0.0.1"
description = "Placeholder package for orditect-bridge-langchain"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "Apache-2.0"}
authors = [{name = "Orditect"}]
