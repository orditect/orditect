[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure logic tests, no external dependencies
    integration: integration tests (cross-package wiring)
    golden: protocol snapshot tests